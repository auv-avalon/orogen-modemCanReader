# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(modem_canTaskLib)
ADD_LIBRARY(${MODEM_CAN_TASKLIB_NAME} SHARED 
    ${MODEM_CAN_TASKLIB_SOURCES})

add_dependencies(${MODEM_CAN_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${MODEM_CAN_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${MODEM_CAN_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${MODEM_CAN_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${MODEM_CAN_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${MODEM_CAN_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${MODEM_CAN_TASKLIB_HEADERS}
    DESTINATION include/orocos/modem_can)

